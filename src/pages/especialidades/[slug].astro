---
import type { GetStaticPaths } from 'astro';
import ServiceLayout from '../../layouts/ServiceLayout.astro';
import { servicesExtended } from '../../data/services-extended';

export const getStaticPaths = (() => {
  const especialidadeServices = servicesExtended.filter(
    service => service.category === 'especialidade'
  );

  return especialidadeServices.map(service => ({
    params: { slug: service.slug },
    props: { service, category: 'especialidades' }
  }));
}) satisfies GetStaticPaths;

const { service, category } = Astro.props;
---

<ServiceLayout service={service} category={category} />
