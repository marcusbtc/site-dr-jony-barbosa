---
import CategoryLayout from '../../layouts/CategoryLayout.astro';
import { serviceCategories } from '../../data/categories';
import { getAllServicesByCategory } from '../../data/services-extended';

const category = serviceCategories.find(cat => cat.slug === 'servicos');
if (!category) throw new Error('Categoria servicos não encontrada');

const services = getAllServicesByCategory('servicos');
---

<CategoryLayout category={category} services={services} />
